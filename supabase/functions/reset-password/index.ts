Deno.serve(async () => {
  const supabaseUrl = Deno.env.get('SUPABASE_URL') ?? ''
  const publishableKeys = JSON.parse(Deno.env.get('SUPABASE_PUBLISHABLE_KEYS') ?? '{}')
  const publishableKey = publishableKeys.default ?? ''
  const html = `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Redefinir senha — Finança Simples</title><style>body{margin:0;background:#07100d;color:#edf8f4;font-family:Inter,system-ui,sans-serif;display:grid;min-height:100vh;place-items:center}.card{width:min(430px,calc(100vw - 32px));background:#0b1713;border:1px solid #1d352c;border-radius:18px;padding:28px;box-sizing:border-box}.mark{width:54px;height:54px;border-radius:16px;display:grid;place-items:center;background:#39d6a6;color:#041a13;font-weight:900;margin-bottom:18px}h1{font-size:22px;margin:0 0 8px}p{color:#8fa99f;line-height:1.5}label{display:grid;gap:7px;margin:16px 0;color:#9bb4aa;font-size:12px;font-weight:700}input{width:100%;box-sizing:border-box;padding:11px;border-radius:9px;border:1px solid #29453a;background:#08120f;color:#edf8f4}button{width:100%;padding:11px;border:0;border-radius:9px;background:#39d6a6;color:#041a13;font-weight:900;cursor:pointer}.msg{min-height:20px;margin-top:14px;font-size:12px;color:#70dfb9}.error{color:#ef9999}</style></head><body><main class="card"><div class="mark">FS</div><h1>Redefinir senha</h1><p>Crie uma nova senha para acessar o Finança Simples.</p><form id="form"><label>Nova senha<input id="password" type="password" minlength="8" required autocomplete="new-password"></label><label>Confirmar senha<input id="confirm" type="password" minlength="8" required autocomplete="new-password"></label><button>Salvar nova senha</button><div id="msg" class="msg"></div></form></main><script type="module">import{createClient}from'https://esm.sh/@supabase/supabase-js@2.95.0';const url=${JSON.stringify(supabaseUrl)},key=${JSON.stringify(publishableKey)},msg=document.getElementById('msg'),form=document.getElementById('form');const client=createClient(url,key,{auth:{detectSessionInUrl:true,persistSession:false}});const set=(t,e=false)=>{msg.textContent=t;msg.className='msg'+(e?' error':'')};form.addEventListener('submit',async(ev)=>{ev.preventDefault();const p=document.getElementById('password').value,c=document.getElementById('confirm').value;if(p!==c)return set('As senhas não conferem.',true);if(p.length<8)return set('Use pelo menos 8 caracteres.',true);set('Atualizando...');const{error}=await client.auth.updateUser({password:p});if(error)return set(error.message||'Não foi possível redefinir a senha.',true);set('Senha alterada. Você já pode voltar ao Finança Simples e entrar com a nova senha.');form.querySelectorAll('input,button').forEach(el=>el.disabled=true)});client.auth.onAuthStateChange((event)=>{if(event==='PASSWORD_RECOVERY')set('Link validado. Defina sua nova senha.')});</script></body></html>`
  return new Response(html, { headers: { 'Content-Type': 'text/html; charset=utf-8', 'Cache-Control': 'no-store', 'Referrer-Policy': 'no-referrer', 'X-Content-Type-Options': 'nosniff', 'Content-Security-Policy': "default-src 'self'; script-src 'unsafe-inline' https://esm.sh; connect-src https://*.supabase.co; style-src 'unsafe-inline'; img-src 'none'; object-src 'none'; frame-ancestors 'none'; base-uri 'none'" } })
})
