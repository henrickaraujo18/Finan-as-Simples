{
  "name": "financa-simples-desktop",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "signer:generate": "tauri signer generate"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.5"
  }
}
